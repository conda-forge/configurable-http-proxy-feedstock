{% set version = "2.0.1" %}
{% set node_version = os.environ.get('NODEJS_VERSION') or '6.*' %}

package:
  name: configurable-http-proxy
  version: {{ version }}

source:
  fn: {{ version }}.tar.gz
  url: https://github.com/jupyterhub/configurable-http-proxy/archive/{{ version }}.tar.gz
  sha256: 3bc5638ab935fe5dfc89192fc9c2b2ab657da797999b234f92571b7b1dcb6627

build:
  number: 0
  script: npm install -g .

requirements:
  build:
    - nodejs {{node_version}}
  run:
    - nodejs {{node_version}}

test:
  commands:
    - configurable-http-proxy -h

about:
  home: https://github.com/jupyterhub/configurable-http-proxy
  license: BSD 3-Clause
  license_file: COPYING.md
  summary: node-http-proxy plus a REST API

extra:
  recipe-maintainers:
    - minrk
